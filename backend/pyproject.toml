[project]
name = "friends-app-backend"
version = "0.1.0"
description = "FastAPI service for the Friends App (digest, betting, voice memos)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
